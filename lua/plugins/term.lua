return {
  'akinsho/toggleterm.nvim',
  version = "*",
  opts = {
    open_mapping = [[<c-\>]],
    hide_numbers = true,
    start_in_insert = true,
    direction = "float",
    shell = vim.o.shell,
  }
}
